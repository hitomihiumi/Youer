--- a/net/minecraft/network/chat/Component.java
+++ b/net/minecraft/network/chat/Component.java
@@ -23,7 +_,19 @@
 import net.minecraft.util.FormattedCharSequence;
 import net.minecraft.world.level.ChunkPos;
 
-public interface Component extends Message, FormattedText {
+public interface Component extends Message, FormattedText, Iterable<Component> { // CraftBukkit
+
+    // CraftBukkit start
+    default java.util.stream.Stream<Component> stream() {
+        return com.google.common.collect.Streams.concat(java.util.stream.Stream.of(this), this.getSiblings().stream().flatMap(Component::stream));
+    }
+
+    @Override
+    default java.util.Iterator<Component> iterator() {
+        return this.stream().iterator();
+    }
+    // CraftBukkit end
+
     Style getStyle();
 
     ComponentContents getContents();
@@ -132,6 +_,11 @@
     }
 
     static MutableComponent literal(String p_237114_) {
+        // Youer - expand Youer's colour tokens, but only for text that can actually carry one:
+        // literal() is hot enough that loading ColorAPI for every plain string would be wasteful
+        if (p_237114_ != null && (p_237114_.indexOf('&') >= 0 || p_237114_.indexOf('<') >= 0)) {
+            return MutableComponent.create(PlainTextContents.create(com.mohistmc.youer.api.ColorAPI.string(p_237114_)));
+        }
         return MutableComponent.create(PlainTextContents.create(p_237114_));
     }
 
