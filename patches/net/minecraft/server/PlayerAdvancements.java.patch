--- a/net/minecraft/server/PlayerAdvancements.java
+++ b/net/minecraft/server/PlayerAdvancements.java
@@ -48,7 +_,7 @@
 
 public class PlayerAdvancements {
     private static final Logger LOGGER = LogUtils.getLogger();
-    private static final Gson GSON = new GsonBuilder().setPrettyPrinting().create();
+    private static final Gson GSON = new GsonBuilder().create(); // Paper - Remove pretty printing from advancements
     private final PlayerList playerList;
     private final Path playerSavePath;
     private AdvancementTree tree;
@@ -61,15 +_,43 @@
     private AdvancementHolder lastSelectedTab;
     private boolean isFirstPacket = true;
     private final Codec<PlayerAdvancements.Data> codec;
+    public final Map<net.minecraft.advancements.critereon.SimpleCriterionTrigger<?>, Set<CriterionTrigger.Listener<?>>> criterionData = new java.util.IdentityHashMap<>(); // Paper - fix advancement data player leakage
+    // Youer start - asynchronous advancement loading and saving
+    // A dedicated scaling pool parses the (potentially huge) advancement file and serializes saves off
+    // the main thread, so several players logging in at once, or an autosave, do not stall the tick.
+    private static final java.util.concurrent.ThreadPoolExecutor ASYNC_EXECUTOR = new java.util.concurrent.ThreadPoolExecutor(
+        2,
+        Math.min(8, Runtime.getRuntime().availableProcessors()),
+        60L,
+        java.util.concurrent.TimeUnit.SECONDS,
+        io.papermc.paper.util.concurrent.ScalingThreadPool.createUnboundedQueue(),
+        new com.google.common.util.concurrent.ThreadFactoryBuilder().setNameFormat("Youer Advancement - %d").build(),
+        io.papermc.paper.util.concurrent.ScalingThreadPool.defaultReEnqueuePolicy()
+    );
+    private final ServerAdvancementManager advancementManager;
+    private final java.util.concurrent.CompletableFuture<PlayerAdvancements.Data> pendingLoad;
+    private boolean loaded;
+    // Saves for one player are chained, so an autosave and a disconnect can never write out of order.
+    private java.util.concurrent.CompletableFuture<Void> lastSave = java.util.concurrent.CompletableFuture.completedFuture(null);
+    // Youer end
 
     public PlayerAdvancements(DataFixer p_265655_, PlayerList p_265703_, ServerAdvancementManager p_265166_, Path p_265268_, ServerPlayer p_265673_) {
         this.playerList = p_265703_;
         this.playerSavePath = p_265268_;
         this.player = p_265673_;
         this.tree = p_265166_.tree();
+        this.advancementManager = p_265166_; // Youer - kept for the asynchronous apply
         int i = 1343;
         this.codec = DataFixTypes.ADVANCEMENTS.wrapCodec(PlayerAdvancements.Data.CODEC, p_265655_, 1343);
-        this.load(p_265166_);
+        // Youer start - parse off the main thread, apply at the sync point (PlayerList#placeNewPlayer)
+        if (com.mohistmc.youer.YouerConfig.async_advancement_loading) {
+            this.pendingLoad = java.util.concurrent.CompletableFuture.supplyAsync(this::parseFile, ASYNC_EXECUTOR);
+        } else {
+            this.pendingLoad = java.util.concurrent.CompletableFuture.completedFuture(null);
+            this.loaded = true;
+            this.load(p_265166_);
+        }
+        // Youer end
     }
 
     public void setPlayer(ServerPlayer p_135980_) {
@@ -83,6 +_,7 @@
     }
 
     public void reload(ServerAdvancementManager p_135982_) {
+        this.ensureLoaded(); // Youer - wait for the pending parse before resetting the state
         this.stopListening();
         this.progress.clear();
         this.visible.clear();
@@ -110,12 +_,19 @@
         }
     }
 
-    private void load(ServerAdvancementManager p_136007_) {
+    // Youer - kept for reload and for subclasses that override it to do nothing
+    protected void load(ServerAdvancementManager p_136007_) {
+        this.applyLoadedData(p_136007_, this.parseFile());
+    }
+
+    // Youer - runs on a worker thread: file access and codec parsing only, no shared state, no events.
+    // Returns null (and logs) when the file is missing or unreadable, exactly like the old sync load.
+    @Nullable
+    private PlayerAdvancements.Data parseFile() {
         if (Files.isRegularFile(this.playerSavePath)) {
             try (Reader reader = Files.newBufferedReader(this.playerSavePath, StandardCharsets.UTF_8)) {
                 JsonElement jsonelement = StrictJsonParser.parse(reader);
-                PlayerAdvancements.Data playeradvancements$data = this.codec.parse(JsonOps.INSTANCE, jsonelement).getOrThrow(JsonParseException::new);
-                this.applyFrom(p_136007_, playeradvancements$data);
+                return this.codec.parse(JsonOps.INSTANCE, jsonelement).getOrThrow(JsonParseException::new);
             } catch (JsonIOException | IOException ioexception) {
                 LOGGER.error("Couldn't access player advancements in {}", this.playerSavePath, ioexception);
             } catch (JsonParseException jsonparseexception) {
@@ -123,28 +_,113 @@
             }
         }
 
+        return null;
+    }
+
+    // Youer - applies the parsed data on the calling thread; must run on the main thread, since it
+    // fires events, grants rewards and registers trigger listeners.
+    private void applyLoadedData(ServerAdvancementManager p_136007_, @Nullable PlayerAdvancements.Data data) {
+        if (data != null) {
+            this.applyFrom(p_136007_, data);
+        }
+
         this.checkForAutomaticTriggers(p_136007_);
         this.registerListeners(p_136007_);
     }
 
+    /**
+     * Youer - waits for the asynchronous parse and applies the data on the calling thread. The parse
+     * never depends on the main thread, so this cannot deadlock; in the normal login flow it finishes
+     * while the client is still configuring and this returns immediately.
+     */
+    public void ensureLoaded() {
+        if (this.loaded) {
+            return;
+        }
+        this.loaded = true;
+        PlayerAdvancements.Data data = null;
+        try {
+            data = this.pendingLoad.get();
+        } catch (InterruptedException interruptedexception) {
+            Thread.currentThread().interrupt();
+            LOGGER.error("Interrupted while loading player advancements in {}", this.playerSavePath, interruptedexception);
+        } catch (java.util.concurrent.ExecutionException executionexception) {
+            LOGGER.error("Failed to load player advancements in {}", this.playerSavePath, executionexception);
+        }
+
+        this.applyLoadedData(this.advancementManager, data);
+    }
+
     public void save() {
-        JsonElement jsonelement = this.codec.encodeStart(JsonOps.INSTANCE, this.asData()).getOrThrow();
+        if (org.spigotmc.SpigotConfig.disableAdvancementSaving) return; // Spigot
+        this.ensureLoaded(); // Youer - never overwrite the file with data that has not been loaded yet
+        if (com.mohistmc.youer.YouerConfig.async_advancement_saving) {
+            // Youer start - snapshot on the main thread (a cheap deep copy), then encode and write on a
+            // worker; the worker never touches the live progress the main thread keeps mutating
+            PlayerAdvancements.Data snapshot = this.asData();
+            this.lastSave = this.lastSave.thenRunAsync(() -> this.writeSnapshot(snapshot), ASYNC_EXECUTOR)
+                .handle((p_youer0_, p_youer1_) -> {
+                    if (p_youer1_ != null) {
+                        LOGGER.error("Couldn't save player advancements to {}", this.playerSavePath, p_youer1_);
+                    }
+
+                    return null;
+                });
+            // Youer end
+        } else {
+            JsonElement jsonelement = this.codec.encodeStart(JsonOps.INSTANCE, this.asData()).getOrThrow();
+            this.writeJson(jsonelement);
+        }
+    }
+
+    // Youer start - blocks until the last submitted save has hit disk; used on the disconnect path and
+    // by PlayerList#saveAll on the calling (autosave) thread, never on the tick path
+    public void flushSave() {
+        try {
+            this.lastSave.join();
+        } catch (java.util.concurrent.CompletionException completionexception) {
+            // Already logged by the handle() above; the next save rewrites the file.
+        }
+    }
+
+    private void writeSnapshot(PlayerAdvancements.Data snapshot) {
+        this.writeJson(this.codec.encodeStart(JsonOps.INSTANCE, snapshot).getOrThrow());
+    }
+
+    private void writeJson(JsonElement jsonelement) {
+        Path tmp = this.playerSavePath.resolveSibling(this.playerSavePath.getFileName() + ".tmp");
 
         try {
             FileUtil.createDirectoriesSafe(this.playerSavePath.getParent());
 
-            try (Writer writer = Files.newBufferedWriter(this.playerSavePath, StandardCharsets.UTF_8)) {
+            try (Writer writer = Files.newBufferedWriter(tmp, StandardCharsets.UTF_8)) {
                 GSON.toJson(jsonelement, GSON.newJsonWriter(writer));
             }
+
+            // Replace atomically: with asynchronous saves the server can die between the encode and the
+            // write, and a torn target file must never be left behind.
+            try {
+                Files.move(tmp, this.playerSavePath, java.nio.file.StandardCopyOption.REPLACE_EXISTING, java.nio.file.StandardCopyOption.ATOMIC_MOVE);
+            } catch (java.nio.file.AtomicMoveNotSupportedException atomicmovenotsupportedexception) {
+                Files.move(tmp, this.playerSavePath, java.nio.file.StandardCopyOption.REPLACE_EXISTING);
+            }
         } catch (JsonIOException | IOException ioexception) {
             LOGGER.error("Couldn't save player advancements to {}", this.playerSavePath, ioexception);
+
+            try {
+                Files.deleteIfExists(tmp);
+            } catch (IOException ioexception1) {
+            }
         }
     }
+    // Youer end
 
     private void applyFrom(ServerAdvancementManager p_301166_, PlayerAdvancements.Data p_301198_) {
         p_301198_.forEach((p_300732_, p_300733_) -> {
             AdvancementHolder advancementholder = p_301166_.get(p_300732_);
             if (advancementholder == null) {
+                if (!p_300732_.getNamespace().equals(ResourceLocation.DEFAULT_NAMESPACE)) return; // CraftBukkit
+                if (!org.purpurmc.purpur.PurpurConfig.loggerSuppressIgnoredAdvancementWarnings) // Purpur - Logger settings (suppressing pointless logs)
                 LOGGER.warn("Ignored advancement '{}' in progress file {} - it doesn't exist anymore?", p_300732_, this.playerSavePath);
             } else {
                 this.startProgress(advancementholder, p_300733_);
@@ -158,32 +_,56 @@
         Map<ResourceLocation, AdvancementProgress> map = new LinkedHashMap<>();
         this.progress.forEach((p_300724_, p_300725_) -> {
             if (p_300725_.hasProgress()) {
-                map.put(p_300724_.id(), p_300725_);
+                // Youer - deep copy: a worker thread encodes this snapshot while the main thread keeps
+                // mutating the live progress, so no mutable state may be shared with it
+                map.put(p_300724_.id(), p_300725_.snapshot());
             }
         });
         return new PlayerAdvancements.Data(map);
     }
 
-    public boolean award(AdvancementHolder p_300979_, String p_135990_) {
+    public boolean award(AdvancementHolder advancement, String criterionKey) {
+        // Forge: don't grant advancements for fake players
+        if (this.player instanceof net.neoforged.neoforge.common.util.FakePlayer) return false;
         boolean flag = false;
-        AdvancementProgress advancementprogress = this.getOrStartProgress(p_300979_);
-        boolean flag1 = advancementprogress.isDone();
-        if (advancementprogress.grantProgress(p_135990_)) {
-            this.unregisterListeners(p_300979_);
-            this.progressChanged.add(p_300979_);
+        AdvancementProgress orStartProgress = this.getOrStartProgress(advancement);
+        boolean isDone = orStartProgress.isDone();
+        if (orStartProgress.grantProgress(criterionKey)) {
+            // Paper start - Add PlayerAdvancementCriterionGrantEvent
+            if (!new com.destroystokyo.paper.event.player.PlayerAdvancementCriterionGrantEvent(this.player.getBukkitEntity(), advancement.toBukkit(), criterionKey).callEvent()) {
+                orStartProgress.revokeProgress(criterionKey);
+                return false;
+            }
+            // Paper end - Add PlayerAdvancementCriterionGrantEvent
+            this.unregisterListeners(advancement);
+            this.progressChanged.add(advancement);
             flag = true;
-            if (!flag1 && advancementprogress.isDone()) {
-                p_300979_.value().rewards().grant(this.player);
-                p_300979_.value().display().ifPresent(p_423203_ -> {
-                    if (p_423203_.shouldAnnounceChat() && this.player.level().getGameRules().getBoolean(GameRules.RULE_ANNOUNCE_ADVANCEMENTS)) {
-                        this.playerList.broadcastSystemMessage(p_423203_.getType().createAnnouncement(p_300979_, this.player), false);
+            net.neoforged.neoforge.event.EventHooks.onAdvancementProgressedEvent(this.player, advancement, orStartProgress, criterionKey, net.neoforged.neoforge.event.entity.player.AdvancementEvent.AdvancementProgressEvent.ProgressType.GRANT);
+            if (!isDone && orStartProgress.isDone()) {
+                // Paper start - Add Adventure message to PlayerAdvancementDoneEvent
+                final net.kyori.adventure.text.Component message = advancement.value().display().flatMap(info -> {
+                    return java.util.Optional.ofNullable(
+                        info.shouldAnnounceChat() ? io.papermc.paper.adventure.PaperAdventure.asAdventure(info.getType().createAnnouncement(advancement, this.player)) : null
+                    );
+                }).orElse(null);
+                final org.bukkit.event.player.PlayerAdvancementDoneEvent event = new org.bukkit.event.player.PlayerAdvancementDoneEvent(this.player.getBukkitEntity(), advancement.toBukkit(), message);
+                this.player.level().getCraftServer().getPluginManager().callEvent(event); // CraftBukkit
+                // Paper end
+                advancement.value().rewards().grant(this.player);
+                advancement.value().display().ifPresent(displayInfo -> {
+                    // Paper start - Add Adventure message to PlayerAdvancementDoneEvent
+                    if (event.message() != null && this.player.level().getGameRules().getBoolean(GameRules.RULE_ANNOUNCE_ADVANCEMENTS)) {
+                        if (org.purpurmc.purpur.PurpurConfig.advancementOnlyBroadcastToAffectedPlayer) this.player.sendMessage(message); else // Purpur - Configurable broadcast settings
+                        this.playerList.broadcastSystemMessage(io.papermc.paper.adventure.PaperAdventure.asVanilla(event.message()), false);
+                        // Paper end
                     }
+                    net.neoforged.neoforge.event.EventHooks.onAdvancementEarnedEvent(this.player, advancement);
                 });
             }
         }
 
-        if (!flag1 && advancementprogress.isDone()) {
-            this.markForVisibilityUpdate(p_300979_);
+        if (!isDone && orStartProgress.isDone()) {
+            this.markForVisibilityUpdate(advancement);
         }
 
         return flag;
@@ -197,6 +_,7 @@
             this.registerListeners(p_301073_);
             this.progressChanged.add(p_301073_);
             flag = true;
+            net.neoforged.neoforge.event.EventHooks.onAdvancementProgressedEvent(this.player, p_301073_, advancementprogress, p_136000_, net.neoforged.neoforge.event.entity.player.AdvancementEvent.AdvancementProgressEvent.ProgressType.REVOKE);
         }
 
         if (flag1 && !advancementprogress.isDone()) {
@@ -247,7 +_,7 @@
     public void flushDirty(ServerPlayer p_135993_, boolean p_393554_) {
         if (this.isFirstPacket || !this.rootsToUpdate.isEmpty() || !this.progressChanged.isEmpty()) {
             Map<ResourceLocation, AdvancementProgress> map = new HashMap<>();
-            Set<AdvancementHolder> set = new HashSet<>();
+            Set<AdvancementHolder> set = new java.util.TreeSet<>(java.util.Comparator.comparing(adv -> adv.id().toString())); // Paper - Changed from HashSet to TreeSet ordered alphabetically.
             Set<ResourceLocation> set1 = new HashSet<>();
 
             for (AdvancementNode advancementnode : this.rootsToUpdate) {
@@ -317,8 +_,54 @@
         );
     }
 
+    // Youer start - fast advancement map codec
+    // The vanilla unboundedMap codec collects into an Object2ObjectArrayMap whose putIfAbsent does a
+    // linear scan, i.e. O(n^2) overall. Large modpack advancement files (tens of thousands of entries)
+    // made that dominate login time. Decode into an open hash map with a pre-sized capacity instead;
+    // encoding keeps using the vanilla codec, where the cost does not matter.
+    private static final Codec<Map<ResourceLocation, AdvancementProgress>> UNBOUNDED_MAP_CODEC = Codec.unboundedMap(ResourceLocation.CODEC, AdvancementProgress.CODEC);
+    private static final Codec<Map<ResourceLocation, AdvancementProgress>> FAST_MAP_CODEC = new Codec<>() {
+        @Override
+        public <T> com.mojang.serialization.DataResult<com.mojang.datafixers.util.Pair<Map<ResourceLocation, AdvancementProgress>, T>> decode(
+            com.mojang.serialization.DynamicOps<T> ops, T input
+        ) {
+            return ops.getMapValues(input).flatMap(stream -> {
+                java.util.List<com.mojang.datafixers.util.Pair<T, T>> list = stream.toList();
+                it.unimi.dsi.fastutil.objects.Object2ObjectOpenHashMap<ResourceLocation, AdvancementProgress> map =
+                    new it.unimi.dsi.fastutil.objects.Object2ObjectOpenHashMap<>(Math.max(16, list.size() + 1));
+
+                for (com.mojang.datafixers.util.Pair<T, T> entry : list) {
+                    com.mojang.serialization.DataResult<ResourceLocation> keyResult = ResourceLocation.CODEC.parse(ops, entry.getFirst());
+                    if (keyResult.error().isPresent()) {
+                        return com.mojang.serialization.DataResult.error(() -> "Failed to parse advancement id: " + keyResult.error().get().message());
+                    }
+
+                    com.mojang.serialization.DataResult<AdvancementProgress> valueResult = AdvancementProgress.CODEC.parse(ops, entry.getSecond());
+                    if (valueResult.error().isPresent()) {
+                        return com.mojang.serialization.DataResult.error(
+                            () -> "Failed to parse advancement progress for '" + keyResult.getOrThrow() + "': " + valueResult.error().get().message()
+                        );
+                    }
+
+                    ResourceLocation key = keyResult.getOrThrow();
+                    if (map.putIfAbsent(key, valueResult.getOrThrow()) != null) {
+                        return com.mojang.serialization.DataResult.error(() -> "Duplicate advancement id: " + key);
+                    }
+                }
+
+                return com.mojang.serialization.DataResult.success(com.mojang.datafixers.util.Pair.of(map, ops.empty()));
+            });
+        }
+
+        @Override
+        public <T> com.mojang.serialization.DataResult<T> encode(Map<ResourceLocation, AdvancementProgress> map, com.mojang.serialization.DynamicOps<T> ops, T prefix) {
+            return UNBOUNDED_MAP_CODEC.encode(map, ops, prefix);
+        }
+    };
+    // Youer end
+
     record Data(Map<ResourceLocation, AdvancementProgress> map) {
-        public static final Codec<PlayerAdvancements.Data> CODEC = Codec.unboundedMap(ResourceLocation.CODEC, AdvancementProgress.CODEC)
+        public static final Codec<PlayerAdvancements.Data> CODEC = FAST_MAP_CODEC
             .xmap(PlayerAdvancements.Data::new, PlayerAdvancements.Data::map);
 
         public void forEach(BiConsumer<ResourceLocation, AdvancementProgress> p_300973_) {
