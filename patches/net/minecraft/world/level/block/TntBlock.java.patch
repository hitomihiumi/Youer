--- a/net/minecraft/world/level/block/TntBlock.java
+++ b/net/minecraft/world/level/block/TntBlock.java
@@ -47,7 +_,8 @@
     @Override
     protected void onPlace(BlockState p_57466_, Level p_57467_, BlockPos p_57468_, BlockState p_57469_, boolean p_57470_) {
         if (!p_57469_.is(p_57466_.getBlock())) {
-            if (p_57467_.hasNeighborSignal(p_57468_) && prime(p_57467_, p_57468_)) {
+            // CraftBukkit/Paper - TNTPrimeEvent check happens before NeoForge's onCaughtFire hook fires.
+            if (p_57467_.hasNeighborSignal(p_57468_) && org.bukkit.craftbukkit.event.CraftEventFactory.callTNTPrimeEvent(p_57467_, p_57468_, org.bukkit.event.block.TNTPrimeEvent.PrimeCause.REDSTONE, null, null) && onCaughtFire(p_57466_, p_57467_, p_57468_, null, null)) {
                 p_57467_.removeBlock(p_57468_, false);
             }
         }
@@ -55,7 +_,8 @@
 
     @Override
     protected void neighborChanged(BlockState p_57457_, Level p_57458_, BlockPos p_57459_, Block p_57460_, @Nullable Orientation p_364510_, boolean p_57462_) {
-        if (p_57458_.hasNeighborSignal(p_57459_) && prime(p_57458_, p_57459_)) {
+        // CraftBukkit/Paper - TNTPrimeEvent check happens before NeoForge's onCaughtFire hook fires.
+        if (p_57458_.hasNeighborSignal(p_57459_) && org.bukkit.craftbukkit.event.CraftEventFactory.callTNTPrimeEvent(p_57458_, p_57459_, org.bukkit.event.block.TNTPrimeEvent.PrimeCause.REDSTONE, null, null) && onCaughtFire(p_57457_, p_57458_, p_57459_, null, null)) {
             p_57458_.removeBlock(p_57459_, false);
         }
     }
@@ -63,7 +_,10 @@
     @Override
     public BlockState playerWillDestroy(Level p_57445_, BlockPos p_57446_, BlockState p_57447_, Player p_57448_) {
         if (!p_57445_.isClientSide() && !p_57448_.getAbilities().instabuild && p_57447_.getValue(UNSTABLE)) {
-            prime(p_57445_, p_57446_);
+            // CraftBukkit/Paper - TNTPrimeEvent check happens before NeoForge's onCaughtFire hook fires.
+            if (org.bukkit.craftbukkit.event.CraftEventFactory.callTNTPrimeEvent(p_57445_, p_57446_, org.bukkit.event.block.TNTPrimeEvent.PrimeCause.BLOCK_BREAK, p_57448_, null)) {
+                onCaughtFire(p_57447_, p_57445_, p_57446_, null, null);
+            }
         }
 
         return super.playerWillDestroy(p_57445_, p_57446_, p_57447_, p_57448_);
@@ -79,16 +_,25 @@
         }
     }
 
+    /** @deprecated Neo: use {@link net.neoforged.neoforge.common.extensions.IBlockStateExtension#onCaughtFire(Level,BlockPos,net.minecraft.core.Direction,LivingEntity)} instead */
+    @Deprecated
     public static boolean prime(Level p_57437_, BlockPos p_57438_) {
-        return prime(p_57437_, p_57438_, null);
+    // Paper start
+        return prime(p_57437_, p_57438_, null, () -> true);
+    }
+    public static boolean prime(Level level, BlockPos pos, java.util.function.BooleanSupplier event) {
+        return prime(level, pos, null, event);
+    // Paper end
     }
 
-    private static boolean prime(Level p_394104_, BlockPos p_393490_, @Nullable LivingEntity p_411047_) {
-        if (p_394104_ instanceof ServerLevel serverlevel && serverlevel.getGameRules().getBoolean(GameRules.RULE_TNT_EXPLODES)) {
-            PrimedTnt primedtnt = new PrimedTnt(p_394104_, p_393490_.getX() + 0.5, p_393490_.getY(), p_393490_.getZ() + 0.5, p_411047_);
-            p_394104_.addFreshEntity(primedtnt);
-            p_394104_.playSound(null, primedtnt.getX(), primedtnt.getY(), primedtnt.getZ(), SoundEvents.TNT_PRIMED, SoundSource.BLOCKS, 1.0F, 1.0F);
-            p_394104_.gameEvent(p_411047_, GameEvent.PRIME_FUSE, p_393490_);
+    /** @deprecated Neo: use {@link net.neoforged.neoforge.common.extensions.IBlockStateExtension#onCaughtFire(Level,BlockPos,net.minecraft.core.Direction,LivingEntity)} instead */
+    @Deprecated
+    private static boolean prime(Level level, BlockPos pos, @Nullable LivingEntity entity, java.util.function.BooleanSupplier event) { // Paper
+        if (level instanceof ServerLevel serverLevel && serverLevel.getGameRules().getBoolean(GameRules.RULE_TNT_EXPLODES) && event.getAsBoolean()) { // Paper
+            PrimedTnt primedTnt = new PrimedTnt(level, pos.getX() + 0.5, pos.getY(), pos.getZ() + 0.5, entity);
+            level.addFreshEntity(primedTnt);
+            level.playSound(null, primedTnt.getX(), primedTnt.getY(), primedTnt.getZ(), SoundEvents.TNT_PRIMED, SoundSource.BLOCKS, 1.0F, 1.0F);
+            level.gameEvent(entity, GameEvent.PRIME_FUSE, pos);
             return true;
         } else {
             return false;
@@ -102,7 +_,8 @@
         if (!p_316149_.is(Items.FLINT_AND_STEEL) && !p_316149_.is(Items.FIRE_CHARGE)) {
             return super.useItemOn(p_316149_, p_316217_, p_316520_, p_316601_, p_316770_, p_316393_, p_316532_);
         } else {
-            if (prime(p_316520_, p_316601_, p_316770_)) {
+            // CraftBukkit/Paper - TNTPrimeEvent check happens before NeoForge's onCaughtFire hook fires.
+            if (org.bukkit.craftbukkit.event.CraftEventFactory.callTNTPrimeEvent(p_316520_, p_316601_, org.bukkit.event.block.TNTPrimeEvent.PrimeCause.PLAYER, p_316770_, null) && onCaughtFire(p_316217_, p_316520_, p_316601_, p_316532_.getDirection(), p_316770_)) {
                 p_316520_.setBlock(p_316601_, Blocks.AIR.defaultBlockState(), 11);
                 Item item = p_316149_.getItem();
                 if (p_316149_.is(Items.FLINT_AND_STEEL)) {
@@ -126,9 +_,12 @@
         if (p_57429_ instanceof ServerLevel serverlevel) {
             BlockPos blockpos = p_57431_.getBlockPos();
             Entity entity = p_57432_.getOwner();
+            // CraftBukkit - EntityChangeBlockEvent / TNTPrimeEvent check happens before NeoForge's onCaughtFire hook fires.
             if (p_57432_.isOnFire()
                 && p_57432_.mayInteract(serverlevel, blockpos)
-                && prime(p_57429_, blockpos, entity instanceof LivingEntity ? (LivingEntity)entity : null)) {
+                && org.bukkit.craftbukkit.event.CraftEventFactory.callEntityChangeBlockEvent(p_57432_, blockpos, p_57430_.getFluidState().createLegacyBlock())
+                && org.bukkit.craftbukkit.event.CraftEventFactory.callTNTPrimeEvent(p_57429_, blockpos, org.bukkit.event.block.TNTPrimeEvent.PrimeCause.PROJECTILE, p_57432_, null)
+                && onCaughtFire(p_57430_, p_57429_, blockpos, null, entity instanceof LivingEntity ? (LivingEntity)entity : null)) {
                 p_57429_.removeBlock(blockpos, false);
             }
         }
@@ -142,5 +_,10 @@
     @Override
     protected void createBlockStateDefinition(StateDefinition.Builder<Block, BlockState> p_57464_) {
         p_57464_.add(UNSTABLE);
+    }
+
+    @Override
+    public boolean onCaughtFire(BlockState state, Level world, BlockPos pos, @Nullable net.minecraft.core.Direction face, @Nullable LivingEntity igniter) {
+        return prime(world, pos, igniter, () -> true);
     }
 }
