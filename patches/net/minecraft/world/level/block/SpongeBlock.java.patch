--- a/net/minecraft/world/level/block/SpongeBlock.java
+++ b/net/minecraft/world/level/block/SpongeBlock.java
@@ -51,10 +_,12 @@
     }
 
     private boolean removeWaterBreadthFirstSearch(Level p_56808_, BlockPos p_56809_) {
-        return BlockPos.breadthFirstTraversal(
+        BlockState spongeState = p_56808_.getBlockState(p_56809_);
+        org.bukkit.craftbukkit.util.BlockStateListPopulator blockList = new org.bukkit.craftbukkit.util.BlockStateListPopulator(p_56808_); // CraftBukkit - Use BlockStateListPopulator
+        BlockPos.breadthFirstTraversal(
                 p_56809_,
-                6,
-                65,
+                p_56808_.purpurConfig.spongeAbsorptionRadius, // Purpur - Configurable sponge absorption
+                p_56808_.purpurConfig.spongeAbsorptionArea, // Purpur - Configurable sponge absorption
                 (p_277519_, p_277492_) -> {
                     for (Direction direction : ALL_DIRECTIONS) {
                         p_277492_.accept(p_277519_.relative(direction));
@@ -64,16 +_,24 @@
                     if (p_294069_.equals(p_56809_)) {
                         return BlockPos.TraversalNodeStatus.ACCEPT;
                     } else {
-                        BlockState blockstate = p_56808_.getBlockState(p_294069_);
-                        FluidState fluidstate = p_56808_.getFluidState(p_294069_);
-                        if (!fluidstate.is(FluidTags.WATER)) {
+                        // CraftBukkit start
+                        BlockState blockstate = blockList.getBlockState(p_294069_);
+                        FluidState fluidstate = blockList.getFluidState(p_294069_);
+                        // CraftBukkit end
+                        if (!spongeState.canBeHydrated(p_56808_, p_56809_, fluidstate, p_294069_)
+                            && (!p_56808_.purpurConfig.spongeAbsorbsLava || !fluidstate.is(FluidTags.LAVA))
+                            && (!p_56808_.purpurConfig.spongeAbsorbsWaterFromMud || !blockstate.is(Blocks.MUD))) { // Purpur - Option for sponges to work on lava and mud
                             return BlockPos.TraversalNodeStatus.SKIP;
                         } else if (blockstate.getBlock() instanceof BucketPickup bucketpickup
-                            && !bucketpickup.pickupBlock(null, p_56808_, p_294069_, blockstate).isEmpty()) {
+                            && !bucketpickup.pickupBlock(null, blockList, p_294069_, blockstate).isEmpty()) { // CraftBukkit
                             return BlockPos.TraversalNodeStatus.ACCEPT;
                         } else {
                             if (blockstate.getBlock() instanceof LiquidBlock) {
-                                p_56808_.setBlock(p_294069_, Blocks.AIR.defaultBlockState(), 3);
+                                blockList.setBlock(p_294069_, Blocks.AIR.defaultBlockState(), 3); // CraftBukkit
+                            // Purpur start - Option for sponges to work on lava and mud
+                            } else if (blockstate.is(Blocks.MUD)) {
+                                blockList.setBlock(p_294069_, Blocks.CLAY.defaultBlockState(), 3);
+                            // Purpur end - Option for sponges to work on lava and mud
                             } else {
                                 if (!blockstate.is(Blocks.KELP)
                                     && !blockstate.is(Blocks.KELP_PLANT)
@@ -82,16 +_,49 @@
                                     return BlockPos.TraversalNodeStatus.SKIP;
                                 }
 
-                                BlockEntity blockentity = blockstate.hasBlockEntity() ? p_56808_.getBlockEntity(p_294069_) : null;
-                                dropResources(blockstate, p_56808_, p_294069_, blockentity);
-                                p_56808_.setBlock(p_294069_, Blocks.AIR.defaultBlockState(), 3);
+                                // CraftBukkit start
+                                // BlockEntity blockEntity = blockState.hasBlockEntity() ? level.getBlockEntity(blockPos) : null;
+                                // dropResources(blockState, level, blockPos, blockEntity);
+                                blockList.setBlock(p_294069_, Blocks.AIR.defaultBlockState(), 3);
+                                // CraftBukkit end
                             }
 
                             return BlockPos.TraversalNodeStatus.ACCEPT;
                         }
                     }
                 }
-            )
-            > 1;
+            );
+        // CraftBukkit start
+        java.util.List<org.bukkit.craftbukkit.block.CraftBlockState> snapshots = blockList.getSnapshotBlocks(); // Is a clone
+        if (!snapshots.isEmpty()) {
+            final org.bukkit.block.Block sponge = org.bukkit.craftbukkit.block.CraftBlock.at(p_56808_, p_56809_);
+
+            org.bukkit.event.block.SpongeAbsorbEvent event = new org.bukkit.event.block.SpongeAbsorbEvent(sponge, (java.util.List<org.bukkit.block.BlockState>) (java.util.List) snapshots);
+            if (!event.callEvent()) {
+                return false;
+            }
+
+            for (org.bukkit.craftbukkit.block.CraftBlockState snapshot : snapshots) {
+                BlockPos blockPos = snapshot.getPosition();
+                BlockState blockState = p_56808_.getBlockState(blockPos);
+                FluidState fluidState = p_56808_.getFluidState(blockPos);
+
+                if (!fluidState.is(FluidTags.WATER)) {
+                } else if (blockState.getBlock() instanceof BucketPickup bucketPickup && !bucketPickup.pickupBlock(null, p_56808_, blockPos, blockState).isEmpty()) {
+                } else if (blockState.getBlock() instanceof LiquidBlock) {
+                } else if (blockState.is(Blocks.KELP) || blockState.is(Blocks.KELP_PLANT) || blockState.is(Blocks.SEAGRASS) || blockState.is(Blocks.TALL_SEAGRASS)) {
+                    BlockEntity blockEntity = blockState.hasBlockEntity() ? p_56808_.getBlockEntity(blockPos) : null;
+
+                    if (snapshot.getHandle().isAir()) {
+                        dropResources(blockState, p_56808_, blockPos, blockEntity);
+                    }
+                }
+                snapshot.place(snapshot.getFlags());
+            }
+
+            return true;
+        }
+        return false;
+        // CraftBukkit end
     }
 }
