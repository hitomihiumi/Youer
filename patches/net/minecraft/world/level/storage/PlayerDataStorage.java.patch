--- a/net/minecraft/world/level/storage/PlayerDataStorage.java
+++ b/net/minecraft/world/level/storage/PlayerDataStorage.java
@@ -42,31 +_,48 @@
             Path path2 = path.resolve(p_78434_.getStringUUID() + ".dat");
             Path path3 = path.resolve(p_78434_.getStringUUID() + ".dat_old");
             Util.safeReplaceFile(path2, path1, path3);
+            net.neoforged.neoforge.event.EventHooks.firePlayerSavingEvent(p_78434_, playerDir, p_78434_.getStringUUID());
         } catch (Exception exception) {
             LOGGER.warn("Failed to save player data for {}", p_78434_.getName().getString());
         }
     }
 
-    private void backup(Player p_316529_, String p_316776_) {
+    private void backup(String name, String stringUuid, String p_316776_) { // CraftBukkit
         Path path = this.playerDir.toPath();
-        Path path1 = path.resolve(p_316529_.getStringUUID() + p_316776_);
-        Path path2 = path.resolve(p_316529_.getStringUUID() + "_corrupted_" + LocalDateTime.now().format(FORMATTER) + p_316776_);
+        Path path1 = path.resolve(stringUuid + p_316776_); // CraftBukkit
+        Path path2 = path.resolve(stringUuid + "_corrupted_" + LocalDateTime.now().format(FORMATTER) + p_316776_); // CraftBukkit
         if (Files.isRegularFile(path1)) {
             try {
                 Files.copy(path1, path2, StandardCopyOption.REPLACE_EXISTING, StandardCopyOption.COPY_ATTRIBUTES);
             } catch (Exception exception) {
-                LOGGER.warn("Failed to copy the player.dat file for {}", p_316529_.getName().getString(), exception);
+                LOGGER.warn("Failed to copy the player.dat file for {}", name, exception); // CraftBukkit
             }
         }
     }
 
-    private Optional<CompoundTag> load(Player p_316534_, String p_316666_) {
-        File file1 = new File(this.playerDir, p_316534_.getStringUUID() + p_316666_);
+    private Optional<CompoundTag> load(String name, String stringUuid, String p_316666_) { // CraftBukkit
+        File file1 = new File(this.playerDir, stringUuid + p_316666_); // CraftBukkit
+        // Spigot start
+        boolean usingWrongFile = false;
+        if (org.bukkit.Bukkit.getOnlineMode() && !file1.exists()) { // Paper - Check online mode first
+            file1 = new File(this.playerDir, java.util.UUID.nameUUIDFromBytes(("OfflinePlayer:" + name).getBytes(java.nio.charset.StandardCharsets.UTF_8)).toString() + p_316666_);
+            if (file1.exists()) {
+                usingWrongFile = true;
+                LOGGER.warn("Using offline mode UUID file for player {} as it is the only copy we can find.", name);
+            }
+        }
+        // Spigot end
         if (file1.exists() && file1.isFile()) {
             try {
-                return Optional.of(NbtIo.readCompressed(file1.toPath(), NbtAccounter.unlimitedHeap()));
+                // Spigot start
+                Optional<CompoundTag> optional = Optional.of(NbtIo.readCompressed(file1.toPath(), NbtAccounter.unlimitedHeap()));
+                if (usingWrongFile) {
+                    file1.renameTo(new File(file1.getPath() + ".offline-read"));
+                }
+                return optional;
+                // Spigot end
             } catch (Exception exception) {
-                LOGGER.warn("Failed to load player data for {}", p_316534_.getName().getString());
+                LOGGER.warn("Failed to load player data for {}", name); // CraftBukkit
             }
         }
 
@@ -74,17 +_,39 @@
     }
 
     public Optional<ValueInput> load(Player p_78436_, ProblemReporter p_421744_) {
-        Optional<CompoundTag> optional = this.load(p_78436_, ".dat");
+        // CraftBukkit start
+        return this.load(p_78436_.getName().getString(), p_78436_.getStringUUID(), p_421744_).map(tag -> {
+            if (p_78436_ instanceof net.minecraft.server.level.ServerPlayer serverPlayer) {
+                org.bukkit.craftbukkit.entity.CraftPlayer craftPlayer = serverPlayer.getBukkitEntity();
+                // Only update first played if it is older than the one we have
+                long modified = new File(this.playerDir, p_78436_.getStringUUID() + ".dat").lastModified();
+                if (modified < craftPlayer.getFirstPlayed()) {
+                    craftPlayer.setFirstPlayed(modified);
+                }
+            }
+
+            ValueInput valueinput = TagValueInput.create(p_421744_, p_78436_.registryAccess(), tag);
+            p_78436_.load(valueinput); // From below
+            net.neoforged.neoforge.event.EventHooks.firePlayerLoadingEvent(p_78436_, playerDir, p_78436_.getStringUUID());
+            return valueinput;
+        });
+    }
+
+    public Optional<CompoundTag> load(String name, String uuid, ProblemReporter p_421744_) {
+        // CraftBukkit end
+        Optional<CompoundTag> optional = this.load(name, uuid, ".dat"); // CraftBukkit
         if (optional.isEmpty()) {
-            this.backup(p_78436_, ".dat");
+            this.backup(name, uuid, ".dat"); // CraftBukkit
         }
 
-        return optional.or(() -> this.load(p_78436_, ".dat_old")).map(p_427223_ -> {
+        return optional.or(() -> this.load(name, uuid, ".dat_old")).map(p_427223_ -> { // CraftBukkit
             int i = NbtUtils.getDataVersion(p_427223_, -1);
             p_427223_ = DataFixTypes.PLAYER.updateToCurrentVersion(this.fixerUpper, p_427223_, i);
-            ValueInput valueinput = TagValueInput.create(p_421744_, p_78436_.registryAccess(), p_427223_);
-            p_78436_.load(valueinput);
-            return valueinput;
+            return p_427223_; // CraftBukkit - handled above
         });
+    }
+
+    public File getPlayerDir() {
+        return playerDir;
     }
 }
