--- a/net/minecraft/world/level/storage/PrimaryLevelData.java
+++ b/net/minecraft/world/level/storage/PrimaryLevelData.java
@@ -39,6 +_,10 @@
 import org.slf4j.Logger;
 
 public class PrimaryLevelData implements ServerLevelData, WorldData {
+    // Youer - cached encode of the world generation settings; see setTagData below
+    @Nullable private Tag cachedWorldGenSettingsTag;
+    @Nullable private net.minecraft.core.Registry<net.minecraft.world.level.dimension.LevelStem> cachedWorldGenSettingsRegistry;
+
     private static final Logger LOGGER = LogUtils.getLogger();
     public static final String LEVEL_NAME = "LevelName";
     protected static final String PLAYER = "Player";
@@ -73,6 +_,22 @@
     private boolean wasModded;
     private final Set<String> removedFeatureFlags;
     private final TimerQueue<MinecraftServer> scheduledEvents;
+    private boolean confirmedExperimentalWarning = false;
+
+    // CraftBukkit start - Add world and pdc
+    public net.minecraft.core.Registry<net.minecraft.world.level.dimension.LevelStem> customDimensions;
+    private net.minecraft.server.level.ServerLevel world;
+    protected net.minecraft.nbt.Tag pdc;
+
+    public void setWorld(net.minecraft.server.level.ServerLevel world) {
+        if (this.world != null) {
+            return;
+        }
+        this.world = world;
+        world.getWorld().readBukkitValues(this.pdc);
+        this.pdc = null;
+    }
+    // CraftBukkit end
 
     private PrimaryLevelData(
         @Nullable CompoundTag p_277888_,
@@ -168,7 +_,7 @@
         Dynamic<T> p_78531_, LevelSettings p_78535_, PrimaryLevelData.SpecialWorldProperty p_250651_, WorldOptions p_251864_, Lifecycle p_78538_
     ) {
         long i = p_78531_.get("Time").asLong(0L);
-        return new PrimaryLevelData(
+        PrimaryLevelData result = new PrimaryLevelData(
             p_78531_.get("Player").flatMap(CompoundTag.CODEC::parse).result().orElse(null),
             p_78531_.get("WasModded").asBoolean(false),
             new BlockPos(p_78531_.get("SpawnX").asInt(0), p_78531_.get("SpawnY").asInt(0), p_78531_.get("SpawnZ").asInt(0)),
@@ -191,7 +_,8 @@
                 .asStream()
                 .flatMap(p_338118_ -> p_338118_.asString().result().stream())
                 .collect(Collectors.toCollection(Sets::newLinkedHashSet)),
-            p_78531_.get("removed_features").asStream().flatMap(p_338117_ -> p_338117_.asString().result().stream()).collect(Collectors.toSet()),
+            // Neo: Append removed modded feature flags
+            updateRemovedFeatureFlags(p_78531_.get("removed_features").asStream().flatMap(p_338117_ -> p_338117_.asString().result().stream()), p_78531_.get("enabled_features").asStream().flatMap(features -> features.asString().result().stream())).collect(Collectors.toSet()),
             new TimerQueue<>(TimerCallbacks.SERVER_CALLBACKS, p_78531_.get("ScheduledEvents").asStream()),
             (CompoundTag)p_78531_.get("CustomBossEvents").orElseEmptyMap().getValue(),
             p_78531_.get("DragonFight").read(EndDragonFight.Data.CODEC).resultOrPartial(LOGGER::error).orElse(EndDragonFight.Data.DEFAULT),
@@ -199,7 +_,11 @@
             p_251864_,
             p_250651_,
             p_78538_
-        );
+        ).withConfirmedWarning(p_78538_ != Lifecycle.stable() && p_78531_.get("confirmedExperimentalSettings").asBoolean(false));
+        // Neo:
+        result.setDayTimeFraction(p_78531_.get("neoDayTimeFraction").asFloat(0f));
+        result.setDayTimePerTick(p_78531_.get("neoDayTimePerTick").asFloat(-1f));
+        return result;
     }
 
     @Override
@@ -228,9 +_,26 @@
         p_78547_.put("Version", compoundtag);
         NbtUtils.addCurrentDataVersion(p_78547_);
         DynamicOps<Tag> dynamicops = p_78546_.createSerializationContext(NbtOps.INSTANCE);
-        WorldGenSettings.encode(dynamicops, this.worldOptions, p_78546_)
-            .resultOrPartial(Util.prefix("WorldGenSettings: ", LOGGER::error))
-            .ifPresent(p_78574_ -> p_78547_.put("WorldGenSettings", p_78574_));
+        // Youer start - cache the encoded world generation settings
+        // The input (worldOptions plus the LEVEL_STEM registry) never changes while the server runs, yet
+        // every autosave re-encodes the whole registry once per dimension, which dominated saveEverything
+        // on big modpacks. The registry reference comparison invalidates the cache if a plugin ever swaps
+        // the dimensions registry. The tag is read-only once stored, so background level.dat writes can
+        // share it safely.
+        net.minecraft.core.Registry<net.minecraft.world.level.dimension.LevelStem> levelStemRegistry =
+            this.customDimensions != null ? this.customDimensions : p_78546_.lookupOrThrow(net.minecraft.core.registries.Registries.LEVEL_STEM);
+        if (com.mohistmc.youer.YouerConfig.cache_worldgen_settings && this.cachedWorldGenSettingsTag != null && this.cachedWorldGenSettingsRegistry == levelStemRegistry) {
+            p_78547_.put("WorldGenSettings", this.cachedWorldGenSettingsTag);
+        } else {
+            WorldGenSettings.encode(dynamicops, this.worldOptions, new net.minecraft.world.level.levelgen.WorldDimensions(levelStemRegistry)) // CraftBukkit
+                .resultOrPartial(Util.prefix("WorldGenSettings: ", LOGGER::error))
+                .ifPresent(p_78574_ -> {
+                    p_78547_.put("WorldGenSettings", p_78574_);
+                    this.cachedWorldGenSettingsTag = p_78574_;
+                    this.cachedWorldGenSettingsRegistry = levelStemRegistry;
+                });
+        }
+        // Youer end
         p_78547_.putInt("GameType", this.settings.gameType().getId());
         p_78547_.putInt("SpawnX", this.spawnPos.getX());
         p_78547_.putInt("SpawnY", this.spawnPos.getY());
@@ -267,6 +_,12 @@
         p_78547_.putInt("WanderingTraderSpawnDelay", this.wanderingTraderSpawnDelay);
         p_78547_.putInt("WanderingTraderSpawnChance", this.wanderingTraderSpawnChance);
         p_78547_.storeNullable("WanderingTraderId", UUIDUtil.CODEC, this.wanderingTraderId);
+
+        // Neo:
+        p_78547_.putString("forgeLifecycle", net.neoforged.neoforge.common.CommonHooks.encodeLifecycle(this.settings.getLifecycle()));
+        p_78547_.putBoolean("confirmedExperimentalSettings", this.confirmedExperimentalWarning);
+        p_78547_.putFloat("neoDayTimeFraction", dayTimeFraction);
+        p_78547_.putFloat("neoDayTimePerTick", dayTimePerTick);
     }
 
     private static ListTag stringCollectionToTag(Set<String> p_277880_) {
@@ -344,6 +_,25 @@
 
     @Override
     public void setThundering(boolean p_78562_) {
+        // Paper start - Add cause to Weather/ThunderChangeEvents
+        this.setThundering(p_78562_, org.bukkit.event.weather.ThunderChangeEvent.Cause.UNKNOWN);
+    }
+    public void setThundering(boolean p_78562_, org.bukkit.event.weather.ThunderChangeEvent.Cause cause) {
+        // Paper end - Add cause to Weather/ThunderChangeEvents
+        // CraftBukkit start
+        if (this.thundering == p_78562_) {
+            return;
+        }
+
+        org.bukkit.World world = org.bukkit.Bukkit.getWorld(this.getLevelName());
+        if (world != null) {
+            org.bukkit.event.weather.ThunderChangeEvent thunder = new org.bukkit.event.weather.ThunderChangeEvent(world, p_78562_, cause); // Paper - Add cause to Weather/ThunderChangeEvents
+            org.bukkit.Bukkit.getServer().getPluginManager().callEvent(thunder);
+            if (thunder.isCancelled()) {
+                return;
+            }
+        }
+        // CraftBukkit end
         this.thundering = p_78562_;
     }
 
@@ -364,6 +_,26 @@
 
     @Override
     public void setRaining(boolean p_78576_) {
+        // Paper start - Add cause to Weather/ThunderChangeEvents
+        this.setRaining(p_78576_, org.bukkit.event.weather.WeatherChangeEvent.Cause.UNKNOWN);
+    }
+
+    public void setRaining(boolean p_78576_, org.bukkit.event.weather.WeatherChangeEvent.Cause cause) {
+        // Paper end - Add cause to Weather/ThunderChangeEvents
+        // CraftBukkit start
+        if (this.raining == p_78576_) {
+            return;
+        }
+
+        org.bukkit.World world = org.bukkit.Bukkit.getWorld(this.getLevelName());
+        if (world != null) {
+            org.bukkit.event.weather.WeatherChangeEvent weather = new org.bukkit.event.weather.WeatherChangeEvent(world, p_78576_, cause); // Paper - Add cause to Weather/ThunderChangeEvents
+            org.bukkit.Bukkit.getServer().getPluginManager().callEvent(weather);
+            if (weather.isCancelled()) {
+                return;
+            }
+        }
+        // CraftBukkit end
         this.raining = p_78576_;
     }
 
@@ -430,6 +_,12 @@
     @Override
     public void setDifficulty(Difficulty p_78521_) {
         this.settings = this.settings.withDifficulty(p_78521_);
+        // CraftBukkit start
+        net.minecraft.network.protocol.game.ClientboundChangeDifficultyPacket packet = new net.minecraft.network.protocol.game.ClientboundChangeDifficultyPacket(this.getDifficulty(), this.isDifficultyLocked());
+        for (net.minecraft.server.level.ServerPlayer player : (java.util.List<net.minecraft.server.level.ServerPlayer>) (java.util.List) this.world.players()) {
+            player.connection.send(packet);
+        }
+        // CraftBukkit end
     }
 
     @Override
@@ -566,10 +_,66 @@
         return this.settings.copy();
     }
 
+    // CraftBukkit start - Check if the name stored in NBT is the correct one
+    public void checkName(String name) {
+        if (!this.settings.levelName.equals(name)) {
+            this.settings.levelName = name;
+        }
+    }
+    // CraftBukkit end
+
+    public boolean hasConfirmedExperimentalWarning() {
+        return this.confirmedExperimentalWarning;
+    }
+
+    public PrimaryLevelData withConfirmedWarning(boolean confirmedWarning) { // Builder-like to not patch ctor
+        this.confirmedExperimentalWarning = confirmedWarning;
+        return this;
+    }
+
     @Deprecated
     public static enum SpecialWorldProperty {
         NONE,
         FLAT,
         DEBUG;
+    }
+
+    // Neo: Variable day time code
+
+    private float dayTimeFraction = 0.0f;
+    private float dayTimePerTick = -1.0f;
+
+    @Override
+    public float getDayTimeFraction() {
+        return dayTimeFraction;
+    }
+
+    @Override
+    public float getDayTimePerTick() {
+        return dayTimePerTick;
+    }
+
+    @Override
+    public void setDayTimeFraction(float dayTimeFraction) {
+        this.dayTimeFraction = dayTimeFraction;
+    }
+
+    @Override
+    public void setDayTimePerTick(float dayTimePerTick) {
+        this.dayTimePerTick = dayTimePerTick;
+    }
+
+    private static java.util.stream.Stream<String> updateRemovedFeatureFlags(java.util.stream.Stream<String> removedFeatures, java.util.stream.Stream<String> enabledFeatures) {
+        var unknownFeatureFlags = new HashSet<net.minecraft.resources.ResourceLocation>();
+        // parses the incoming Stream<String> and spits out unknown flag names (ResourceLocation)
+        // we do not care about the returned FeatureFlagSet, only the flags which do not exist
+        net.minecraft.world.flag.FeatureFlags.REGISTRY.fromNames(enabledFeatures.map(net.minecraft.resources.ResourceLocation::parse).collect(Collectors.toSet()), unknownFeatureFlags::add);
+        // concat the received removed flags with our new additions
+        return java.util.stream.Stream.concat(removedFeatures, unknownFeatureFlags.stream()
+                // we only want modded flags, mojang has datafixers for vanilla flags
+                .filter(java.util.function.Predicate.not(name -> name.getNamespace().equals(net.minecraft.resources.ResourceLocation.DEFAULT_NAMESPACE)))
+                .map(net.minecraft.resources.ResourceLocation::toString))
+                // no duplicates should exist in this stream
+                .distinct();
     }
 }
