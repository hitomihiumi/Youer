--- a/net/minecraft/world/level/chunk/storage/ChunkStorage.java
+++ b/net/minecraft/world/level/chunk/storage/ChunkStorage.java
@@ -38,17 +_,30 @@
         return this.worker.isOldChunkAround(p_223452_, p_223453_);
     }
 
+    // CraftBukkit start
     public CompoundTag upgradeChunkTag(
-        ResourceKey<Level> p_188289_,
+        ResourceKey<net.minecraft.world.level.dimension.LevelStem> p_188289_,
         Supplier<DimensionDataStorage> p_188290_,
         CompoundTag p_188291_,
-        Optional<ResourceKey<MapCodec<? extends ChunkGenerator>>> p_188292_
+        Optional<ResourceKey<MapCodec<? extends ChunkGenerator>>> p_188292_,
+        ChunkPos p_223452_,
+        @Nullable net.minecraft.world.level.LevelAccessor levelAccessor
+        // CraftBukkit end
     ) {
         int i = getVersion(p_188291_);
         if (i == SharedConstants.getCurrentVersion().dataVersion().version()) {
             return p_188291_;
         } else {
             try {
+                // CraftBukkit start
+                if (i < 1466) {
+                    CompoundTag level = p_188291_.getCompoundOrEmpty("Level");
+                    if (level.getBooleanOr("TerrainPopulated", false) && !level.getBooleanOr("LightPopulated", false)) {
+                        // Light is purged updating to 1.14+. We need to set light populated to true so the converter recognizes the chunk as being "full"
+                        level.putBoolean("LightPopulated", true);
+                    }
+                }
+                // CraftBukkit end
                 if (i < 1493) {
                     p_188291_ = DataFixTypes.CHUNK.update(this.fixerUpper, p_188291_, i, 1493);
                     if (p_188291_.getCompound("Level").flatMap(p_409525_ -> p_409525_.getBoolean("hasLegacyStructureData")).orElse(false)) {
@@ -57,8 +_,22 @@
                     }
                 }
 
+                // Spigot start - SPIGOT-6806: Quick and dirty way to prevent below zero generation in old chunks, by setting the status to heightmap instead of empty
+                boolean stopBelowZero = false;
+                boolean belowZeroGenerationInExistingChunks = (levelAccessor != null) ? ((net.minecraft.server.level.ServerLevel) levelAccessor).spigotConfig.belowZeroGenerationInExistingChunks : org.spigotmc.SpigotConfig.belowZeroGenerationInExistingChunks;
+
+                if (i <= 2730 && !belowZeroGenerationInExistingChunks) {
+                    stopBelowZero = "full".equals(p_188291_.getCompound("Level").flatMap(l -> l.getString("Status")).orElse(null));
+                }
+                // Spigot end
+
                 injectDatafixingContext(p_188291_, p_188289_, p_188292_);
                 p_188291_ = DataFixTypes.CHUNK.updateToCurrentVersion(this.fixerUpper, p_188291_, Math.max(1493, i));
+                // Spigot start
+                if (stopBelowZero) {
+                    p_188291_.putString("Status", net.minecraft.core.registries.BuiltInRegistries.CHUNK_STATUS.getKey(net.minecraft.world.level.chunk.status.ChunkStatus.SPAWN).toString());
+                }
+                // Spigot end
                 removeDatafixingContext(p_188291_);
                 NbtUtils.addCurrentDataVersion(p_188291_);
                 return p_188291_;
