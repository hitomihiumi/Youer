--- a/net/minecraft/world/level/chunk/LevelChunkSection.java
+++ b/net/minecraft/world/level/chunk/LevelChunkSection.java
@@ -18,11 +_,11 @@
     public static final int SECTION_HEIGHT = 16;
     public static final int SECTION_SIZE = 4096;
     public static final int BIOME_CONTAINER_BITS = 2;
-    private short nonEmptyBlockCount;
+    short nonEmptyBlockCount; // Paper - package private
     private short tickingBlockCount;
     private short tickingFluidCount;
-    public PalettedContainer<BlockState> states;
-    private PalettedContainerRO<Holder<Biome>> biomes;
+    public final PalettedContainer<BlockState> states;
+    private PalettedContainer<Holder<Biome>> biomes; // CraftBukkit - read/write
 
     private LevelChunkSection(LevelChunkSection p_365080_) {
         this.nonEmptyBlockCount = p_365080_.nonEmptyBlockCount;
@@ -32,15 +_,23 @@
         this.biomes = p_365080_.biomes.copy();
     }
 
-    public LevelChunkSection(PalettedContainer<BlockState> p_282846_, PalettedContainerRO<Holder<Biome>> p_281695_) {
-        this.states = p_282846_;
-        this.biomes = p_281695_;
+    public LevelChunkSection(PalettedContainer<BlockState> states, PalettedContainer<Holder<Biome>> biomes) { // CraftBukkit - read/write
+        this.states = states;
+        this.biomes = biomes;
         this.recalcBlockCounts();
     }
 
+    // Paper start - Anti-Xray - Add parameters
+    @Deprecated @io.papermc.paper.annotation.DoNotUse
     public LevelChunkSection(Registry<Biome> p_282873_) {
+        this(p_282873_, null, null, 0);
+    }
+    public LevelChunkSection(Registry<Biome> biomeRegistry, net.minecraft.world.level.Level level, net.minecraft.world.level.ChunkPos chunkPos, int chunkSectionY) {
+        // Paper end - Anti-Xray
+        // Youer - B3: deferred - PalettedContainer's Anti-Xray preset-states constructor overload isn't merged
+        // yet (a real obfuscation subsystem, not a trivial gap); using the vanilla 3-arg constructor instead
         this.states = new PalettedContainer<>(Block.BLOCK_STATE_REGISTRY, Blocks.AIR.defaultBlockState(), PalettedContainer.Strategy.SECTION_STATES);
-        this.biomes = new PalettedContainer<>(p_282873_.asHolderIdMap(), p_282873_.getOrThrow(Biomes.PLAINS), PalettedContainer.Strategy.SECTION_BIOMES);
+        this.biomes = new PalettedContainer<>(biomeRegistry.asHolderIdMap(), biomeRegistry.getOrThrow(Biomes.PLAINS), PalettedContainer.Strategy.SECTION_BIOMES);
     }
 
     public BlockState getBlockState(int p_62983_, int p_62984_, int p_62985_) {
@@ -48,7 +_,7 @@
     }
 
     public FluidState getFluidState(int p_63008_, int p_63009_, int p_63010_) {
-        return this.states.get(p_63008_, p_63009_, p_63010_).getFluidState();
+        return this.states.get(p_63008_, p_63009_, p_63010_).getFluidState(); // Paper - Perf: Optimise LevelChunk#getFluidState; diff on change - we expect this to be effectively just get(p_63008_, p_63009_, p_63010_).getFluidState(). If this changes we need to check other patches that use BlockBehaviour#getFluidState.
     }
 
     public void acquire() {
@@ -73,25 +_,25 @@
 
         FluidState fluidstate = blockstate.getFluidState();
         FluidState fluidstate1 = p_62995_.getFluidState();
-        if (!blockstate.isAir()) {
+        if (!blockstate.isEmpty()) { // Neo: Fix MC-232360 for modded blocks (Makes modded isAir blocks not be replaced with Blocks.AIR in all-air chunk sections)
             this.nonEmptyBlockCount--;
             if (blockstate.isRandomlyTicking()) {
                 this.tickingBlockCount--;
             }
         }
 
-        if (!fluidstate.isEmpty()) {
+        if (!!fluidstate.isRandomlyTicking()) { // Paper - block counting
             this.tickingFluidCount--;
         }
 
-        if (!p_62995_.isAir()) {
+        if (!p_62995_.isEmpty()) { // Neo: Fix MC-232360 for modded blocks (Makes modded isAir blocks not be replaced with Blocks.AIR in all-air chunk sections)
             this.nonEmptyBlockCount++;
             if (p_62995_.isRandomlyTicking()) {
                 this.tickingBlockCount++;
             }
         }
 
-        if (!fluidstate1.isEmpty()) {
+        if (!!fluidstate1.isRandomlyTicking()) { // Paper - block counting
             this.tickingFluidCount++;
         }
 
@@ -122,7 +_,7 @@
 
             public void accept(BlockState p_204444_, int p_204445_) {
                 FluidState fluidstate = p_204444_.getFluidState();
-                if (!p_204444_.isAir()) {
+                if (!p_204444_.isEmpty()) { // Neo: Fix MC-232360 for modded blocks (Makes modded isAir blocks not be replaced with Blocks.AIR in all-air chunk sections)
                     this.nonEmptyBlockCount += p_204445_;
                     if (p_204444_.isRandomlyTicking()) {
                         this.tickingBlockCount += p_204445_;
@@ -167,10 +_,18 @@
         this.biomes = palettedcontainer;
     }
 
+    // Paper start - Anti-Xray - Add chunk packet info
+    @Deprecated @io.papermc.paper.annotation.DoNotUse
     public void write(FriendlyByteBuf p_63012_) {
-        p_63012_.writeShort(this.nonEmptyBlockCount);
-        this.states.write(p_63012_);
-        this.biomes.write(p_63012_);
+        this.write(p_63012_, null, 0);
+    }
+    public void write(FriendlyByteBuf buffer, io.papermc.paper.antixray.ChunkPacketInfo<BlockState> chunkPacketInfo, int chunkSectionIndex) {
+        // Youer - B3: deferred - PalettedContainer's Anti-Xray-aware write() overload isn't merged yet;
+        // falls back to the vanilla single-arg write (loses Anti-Xray obfuscation, not a correctness issue)
+        buffer.writeShort(this.nonEmptyBlockCount);
+        this.states.write(buffer);
+        this.biomes.write(buffer);
+    // Paper end - Anti-Xray - Add chunk packet info
     }
 
     public int getSerializedSize() {
@@ -184,6 +_,11 @@
     public Holder<Biome> getNoiseBiome(int p_204434_, int p_204435_, int p_204436_) {
         return this.biomes.get(p_204434_, p_204435_, p_204436_);
     }
+    // CraftBukkit start
+    public void setBiome(int x, int y, int z, Holder<Biome> biome) {
+        this.biomes.set(x, y, z, biome);
+    }
+    // CraftBukkit end
 
     public void fillBiomesFromNoise(BiomeResolver p_282075_, Climate.Sampler p_283084_, int p_282310_, int p_281510_, int p_283057_) {
         PalettedContainer<Holder<Biome>> palettedcontainer = this.biomes.recreate();
