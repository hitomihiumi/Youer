--- a/net/minecraft/world/entity/ai/navigation/GroundPathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/GroundPathNavigation.java
@@ -41,10 +_,8 @@
     }
 
     @Override
-    public Path createPath(BlockPos p_26475_, int p_26476_) {
-        LevelChunk levelchunk = this.level
-            .getChunkSource()
-            .getChunkNow(SectionPos.blockToSectionCoord(p_26475_.getX()), SectionPos.blockToSectionCoord(p_26475_.getZ()));
+    public Path createPath(BlockPos p_26475_, @javax.annotation.Nullable Entity entity, int p_26476_) { // Paper - EntityPathfindEvent
+        LevelChunk levelchunk = this.level.getChunkSource().getChunkNow(SectionPos.blockToSectionCoord(p_26475_.getX()), SectionPos.blockToSectionCoord(p_26475_.getZ()));
         if (levelchunk == null) {
             return null;
         } else {
@@ -56,7 +_,7 @@
                 }
 
                 if (blockpos$mutableblockpos.getY() >= this.level.getMinY()) {
-                    return super.createPath(blockpos$mutableblockpos.above(), p_26476_);
+                    return super.createPath(blockpos$mutableblockpos.above(), this.mob, p_26476_); // Paper - EntityPathfindEvent
                 }
 
                 blockpos$mutableblockpos.setY(p_26475_.getY() + 1);
@@ -69,7 +_,7 @@
             }
 
             if (!levelchunk.getBlockState(p_26475_).isSolid()) {
-                return super.createPath(p_26475_, p_26476_);
+                return super.createPath(p_26475_, this.mob, p_26476_); // Paper - EntityPathfindEvent
             } else {
                 BlockPos.MutableBlockPos blockpos$mutableblockpos1 = p_26475_.mutable().move(Direction.UP);
 
@@ -77,14 +_,14 @@
                     blockpos$mutableblockpos1.move(Direction.UP);
                 }
 
-                return super.createPath(blockpos$mutableblockpos1.immutable(), p_26476_);
+                return super.createPath(blockpos$mutableblockpos1.immutable(), this.mob, p_26476_); // Paper - EntityPathfindEvent
             }
         }
     }
 
     @Override
     public Path createPath(Entity p_26465_, int p_26466_) {
-        return this.createPath(p_26465_.blockPosition(), p_26466_);
+        return this.createPath(p_26465_.blockPosition(), p_26465_, p_26466_); // Paper - EntityPathfindEvent
     }
 
     private int getSurfaceY() {
