--- a/net/minecraft/world/item/MapItem.java
+++ b/net/minecraft/world/item/MapItem.java
@@ -54,6 +_,16 @@
 
     @Nullable
     public static MapItemSavedData getSavedData(ItemStack p_42854_, Level p_42855_) {
+        // Neo: Add instance method so that mods can override
+        Item map = p_42854_.getItem();
+        if(map instanceof MapItem) {
+            return ((MapItem)map).getCustomMapData(p_42854_, p_42855_);
+        }
+        return null;
+    }
+
+    @Nullable
+    protected MapItemSavedData getCustomMapData(ItemStack p_42854_, Level p_42855_) {
         MapId mapid = p_42854_.get(DataComponents.MAP_ID);
         return getSavedData(mapid, p_42855_);
     }
@@ -97,8 +_,8 @@
                             int j2 = (j / i + k1 - 64) * i;
                             int k2 = (k / i + l1 - 64) * i;
                             Multiset<MapColor> multiset = LinkedHashMultiset.create();
-                            LevelChunk levelchunk = p_42894_.getChunk(SectionPos.blockToSectionCoord(j2), SectionPos.blockToSectionCoord(k2));
-                            if (!levelchunk.isEmpty()) {
+                            LevelChunk levelchunk = p_42894_.getChunkIfLoaded(SectionPos.blockToSectionCoord(j2), SectionPos.blockToSectionCoord(k2)); // Paper - Maps shouldn't load chunks
+                            if (levelchunk != null && !levelchunk.isEmpty()) { // Paper - Maps shouldn't load chunks
                                 int l2 = 0;
                                 double d1 = 0.0;
                                 if (p_42894_.dimensionType().hasCeiling()) {
@@ -197,6 +_,7 @@
     public static void renderBiomePreviewMap(ServerLevel p_42851_, ItemStack p_42852_) {
         MapItemSavedData mapitemsaveddata = getSavedData(p_42852_, p_42851_);
         if (mapitemsaveddata != null) {
+            mapitemsaveddata.isExplorerMap = true; // Purpur - Explorer Map API
             if (p_42851_.dimension() == mapitemsaveddata.dimension) {
                 int i = 1 << mapitemsaveddata.scale;
                 int j = mapitemsaveddata.centerX;
@@ -206,10 +_,10 @@
                 int i1 = k / i - 64;
                 BlockPos.MutableBlockPos blockpos$mutableblockpos = new BlockPos.MutableBlockPos();
 
-                for (int j1 = 0; j1 < 128; j1++) {
-                    for (int k1 = 0; k1 < 128; k1++) {
-                        Holder<Biome> holder = p_42851_.getBiome(blockpos$mutableblockpos.set((l + k1) * i, 0, (i1 + j1) * i));
-                        aboolean[j1 * 128 + k1] = holder.is(BiomeTags.WATER_ON_MAP_OUTLINES);
+                for (int i5 = 0; i5 < 128; i5++) {
+                    for (int i6 = 0; i6 < 128; i6++) {
+                        Holder<Biome> holder = p_42851_.getUncachedNoiseBiome(net.minecraft.core.QuartPos.fromBlock((l + i6) * i), net.minecraft.core.QuartPos.fromBlock(0), net.minecraft.core.QuartPos.fromBlock((i1 + i5) * i)); // Paper - Perf: Use seed based lookup for treasure maps
+                        aboolean[i5 * 128 + i6] = holder.is(BiomeTags.WATER_ON_MAP_OUTLINES);
                     }
                 }
 
